# App
APP_NAME=Aletheia API
APP_VERSION=0.1.0
DEBUG=true
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=aletheia
POSTGRES_USER=aletheia
POSTGRES_PASSWORD=aletheia_dev_password

# Database URL
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Models
ULTRAFACE_PATH=/app/models/ultraface.onnx
ARCFACE_PATH=/app/models/arcface.onnx
CLASSIFIER_PATH=/app/models/mobilenet.onnx
